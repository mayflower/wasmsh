[test]
name = "amp-greater-both-to-file"
tags = ["redirection", "stderr", "P0"]
tier = "P0"
requires = ["stderr-redirection"]

[input]
script = "nonexistent_cmd &> /all.txt; cat /all.txt"

[expect]
status = 0
stdout_contains = ["command not found"]
