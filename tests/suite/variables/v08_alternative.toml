[test]
name = "alternative-value-when-set"
tags = ["variable-expansion", "P0"]
tier = "P0"
requires = ["variable-assignment", "echo", "parameter-expansion", "parameter-alternative"]

[input]
script = "X=yes; echo ${X:+alt}"

[expect]
status = 0
stdout = "alt\n"
