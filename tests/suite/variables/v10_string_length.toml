[test]
name = "string-length-expansion"
tags = ["variable-expansion", "P0"]
tier = "P0"
requires = ["variable-assignment", "echo", "parameter-expansion", "parameter-length"]

[input]
script = "X=hello; echo ${#X}"

[expect]
status = 0
stdout = "5\n"
