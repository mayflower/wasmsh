[test]
name = "special-variable-argument-count"
tags = ["variable-expansion", "P0"]
tier = "P0"
requires = ["echo", "parameter-expansion"]

[input]
script = "echo $#"

[expect]
status = 0
stdout = "0\n"
