[test]
name = "assignment-with-expansion"
tags = ["variable-expansion", "P0"]
tier = "P0"
requires = ["variable-assignment", "echo", "double-quoting", "parameter-expansion"]

[input]
script = 'X=hello; Y="$X world"; echo $Y'

[expect]
status = 0
stdout = "hello world\n"
