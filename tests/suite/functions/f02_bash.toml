[test]
name = "bash-function-definition"
tags = ["function", "P0"]
tier = "P0"
requires = ["function", "echo"]

[input]
script = "function greet { echo hello; }; greet"

[expect]
status = 0
stdout = "hello\n"
