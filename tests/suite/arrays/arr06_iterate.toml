[test]
name = "indexed-array-iterate"
tags = ["arrays", "P0"]
tier = "P0"
requires = ["indexed-array"]

[input]
script = """
fruits=(apple banana cherry)
for f in "${fruits[@]}"; do
  echo "$f"
done
"""

[expect]
status = 0
stdout = "apple\nbanana\ncherry\n"
