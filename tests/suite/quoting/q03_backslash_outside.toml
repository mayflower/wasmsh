[test]
name = "backslash-escapes-space-outside-quotes"
tags = ["quoting", "P0"]
tier = "P0"
requires = ["backslash-escape", "echo"]

[input]
script = "echo hello\\ world"

[expect]
status = 0
stdout = "hello world\n"
