[test]
name = "empty-quoted-strings-preserved-as-arguments"
tags = ["quoting", "P0"]
tier = "P0"
requires = ["function", "echo"]

[input]
script = "f() { echo $#; }; f \"\" \"\""

[expect]
status = 0
stdout = "2\n"
