[test]
name = "cut-fields"
tags = ["utility", "P0"]
tier = "P0"
requires = ["cut"]

[setup]
files = { "/data.csv" = "a,b,c\nd,e,f\n" }

[input]
script = "cut -d , -f 2 /data.csv"

[expect]
status = 0
stdout = "b\ne\n"
