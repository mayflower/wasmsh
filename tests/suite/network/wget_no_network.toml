[test]
name = "wget without network backend returns error"
tags = ["network", "wget"]
tier = "P0"
requires = ["utilities"]

[input]
script = "wget http://example.com/file.txt"

[expect]
status = 1
stderr_contains = ["network access not available"]
