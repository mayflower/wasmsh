[test]
name = "multiple-assignments"
tags = ["shell-state", "P0"]
tier = "P0"
requires = ["variable-assignment", "echo", "parameter-expansion"]

[input]
script = "A=1; B=2; C=3; echo $A$B$C"

[expect]
status = 0
stdout = "123\n"
