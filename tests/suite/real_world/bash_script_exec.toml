[test]
name = "bash executes a script file"
tags = ["bash", "sh", "script"]
tier = "P0"
requires = ["simple-command"]

[setup]
files = { "/test.sh" = "echo hello from script" }

[input]
script = "bash /test.sh"

[expect]
status = 0
stdout = "hello from script\n"
