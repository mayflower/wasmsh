[test]
name = "empty-vs-unset-dash-only"
tags = ["expansion", "P0"]
tier = "P0"
requires = ["parameter-default", "variable-assignment", "echo"]

[input]
script = 'X=""; echo ${X-default}'

[expect]
status = 0
stdout = "\n"
