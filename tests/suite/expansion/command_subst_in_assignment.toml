[test]
name = "command-substitution-in-assignment"
tags = ["expansion", "P0"]
tier = "P0"
requires = ["command-substitution", "echo", "variable-assignment", "parameter-expansion"]

[input]
script = "X=$(echo world); echo hello $X"

[expect]
status = 0
stdout = "hello world\n"
