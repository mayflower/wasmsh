[test]
name = "tilde-expansion-with-path"
tags = ["expansion", "P0"]
tier = "P0"
requires = ["tilde-expansion", "echo"]

[input]
script = "HOME=/home/user; echo ~/bin"

[expect]
status = 0
stdout = "/home/user/bin\n"
