[test]
name = "param-strip-shortest-prefix"
tags = ["expansion", "P1"]
tier = "P1"
requires = ["parameter-expansion", "parameter-strip"]

[input]
script = 'X=hello.tar.gz; echo ${X#*.}'

[expect]
status = 0
stdout = "tar.gz\n"
