[test]
name = "arithmetic-complex-precedence"
tags = ["expansion", "P0"]
tier = "P0"
requires = ["arithmetic-expansion", "echo"]

[input]
script = "echo $((2 + 3 * 4 - 1))"

[expect]
status = 0
stdout = "13\n"
