[test]
name = "parameter-substitution-replace"
tags = ["expansion", "P0"]
tier = "P0"
requires = ["parameter-substitution", "echo"]

[input]
script = "X=hello; echo ${X/ell/ELL}"

[expect]
status = 0
stdout = "hELLo\n"
