[test]
name = "cut-field-range"
tags = ["utility", "gap"]
tier = "P0"
requires = ["cut"]

[setup]
files = { "/data/test.txt" = "a:b:c:d\n1:2:3:4\n" }

[input]
script = "cut -d : -f 2-3 /data/test.txt"

[expect]
status = 0
stdout = "b:c\n2:3\n"
