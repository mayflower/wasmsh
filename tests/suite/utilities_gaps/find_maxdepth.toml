[test]
name = "find-maxdepth"
tags = ["utility", "gap"]
tier = "P0"
requires = ["find"]

[setup]
files = { "/data/a.txt" = "1\n", "/data/sub/b.txt" = "2\n", "/data/sub/deep/c.txt" = "3\n" }

[input]
script = "find /data -maxdepth 1 -type f"

[expect]
status = 0
stdout = "/data/a.txt\n"
