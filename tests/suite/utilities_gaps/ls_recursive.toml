[test]
name = "ls-recursive"
tags = ["utility", "gap"]
tier = "P0"
requires = ["ls", "mkdir"]

[setup]
files = { "/data/sub/file.txt" = "nested\n" }

[input]
script = "ls -R /data"

[expect]
status = 0
stdout_contains = ["sub"]
