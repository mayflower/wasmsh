[test]
name = "find-print0"
tags = ["utility", "gap"]
tier = "P2"
requires = ["find"]

[setup]
files = { "/data/a.txt" = "1\n" }

[input]
script = "find /data -type f -print0 | tr '\\0' '\\n'"

[expect]
status = 0
stdout_contains = ["/data/a.txt"]
