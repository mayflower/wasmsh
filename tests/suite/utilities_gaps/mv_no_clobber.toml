[test]
name = "mv-no-clobber"
tags = ["utility", "gap"]
tier = "P0"
requires = ["mv", "cat"]

[setup]
files = { "/a.txt" = "source\n", "/b.txt" = "existing\n" }

[input]
script = "mv -n /a.txt /b.txt && cat /b.txt"

[expect]
status = 0
stdout = "existing\n"
