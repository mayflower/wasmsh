[test]
name = "grep-word-match"
tags = ["utility", "gap"]
tier = "P0"
requires = ["grep"]

[setup]
files = { "/data/test.txt" = "cat\nconcatenate\nthe cat sat\n" }

[input]
script = "grep -w cat /data/test.txt"

[expect]
status = 0
stdout = "cat\nthe cat sat\n"
