[test]
name = "prefix-name-expansion"
tags = ["expansion", "P2"]
tier = "P2"
requires = ["prefix-expansion"]

[input]
script = """
APP_NAME=myapp
APP_VERSION=1.0
APP_PORT=8080
for v in ${!APP_*}; do
  echo $v
done | sort
"""

[expect]
status = 0
stdout = "APP_NAME\nAPP_PORT\nAPP_VERSION\n"
