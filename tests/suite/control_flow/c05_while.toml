[test]
name = "while: false condition skips loop body"
tags = ["while", "P0"]
tier = "P0"
requires = ["while", "false", "echo", "variable-assignment"]

[input]
script = "X=0; while false; do echo loop; done; echo done"

[expect]
status = 0
stdout = "done\n"
