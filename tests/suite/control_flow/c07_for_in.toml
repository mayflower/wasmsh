[test]
name = "for-in: iterate over word list"
tags = ["for-in", "P0"]
tier = "P0"
requires = ["for-in", "echo", "parameter-expansion"]

[input]
script = "for x in a b c; do echo $x; done"

[expect]
status = 0
stdout = "a\nb\nc\n"
