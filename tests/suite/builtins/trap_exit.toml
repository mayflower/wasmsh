[test]
name = "trap-exit-handler"
tags = ["builtin", "P1"]
tier = "P1"
requires = ["trap", "trap-exit", "exit"]

[input]
script = "trap 'echo cleanup' EXIT; exit 0"

[expect]
status = 0
stdout = "cleanup\n"
