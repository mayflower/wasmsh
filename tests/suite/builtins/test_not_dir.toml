[test]
name = "test-not-dir"
tags = ["builtin", "test"]
tier = "P0"
requires = ["test", "echo", "mkdir"]

[input]
script = "mkdir /mydir; test ! -d /nonexistent; echo $?"

[expect]
status = 0
stdout = "0\n"
