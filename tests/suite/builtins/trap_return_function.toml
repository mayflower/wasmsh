[test]
name = "trap-return-function"
tags = ["builtin", "P1"]
tier = "P1"
requires = ["trap", "trap-return", "function", "echo", "local"]

[input]
script = """
trap 'echo return:${FUNCNAME}' RETURN
f() { local x=1; echo body; }
f
"""

[expect]
status = 0
stdout = "body\nreturn:f\n"
