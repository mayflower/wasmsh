[test]
name = "arithmetic-division-by-zero"
tags = ["arithmetic", "P0"]
tier = "P0"
requires = ["arithmetic-expansion", "echo"]

[input]
script = "echo $((10 / 0))"

[expect]
status = 0
stdout = "0\n"
